# Terraform CLI config for connected environments.
# Uses the local network mirror first; falls back to the public registry for
# any provider not served by the mirror.

provider_installation {
  network_mirror {
    url     = "https://provider-mirror/"
    include = ["registry.terraform.io/*/*"]
  }

  direct {
    exclude = ["registry.terraform.io/*/*"]
  }
}
