# Terraform CLI config for fully offline environments.
# Uses the internal network mirror service (provider-mirror container).
# No fallback to the public registry — any unlisted provider will fail clearly.

provider_installation {
  network_mirror {
    url     = "https://provider-mirror/"
    include = ["registry.terraform.io/*/*"]
  }
}
